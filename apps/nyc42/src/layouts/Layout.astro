---
import BaseLayout from "@dst/ui/BaseLayout.astro";
import { site } from "../site.config";
import { promoted } from "../sections";
import "../styles/theme.css";

interface Props {
  /** Forwarded to BaseLayout: see its `prose` prop. */
  prose?: boolean;
  title: string;
  description: string;
  jsonLd?: Record<string, unknown>[];
  image?: string;
  noindex?: boolean;
}
const { title, description, jsonLd, image, noindex, prose } = Astro.props;

const nav = [
  { href: "/", label: "What's on", title: "What's on in New York today", icon: "home" },
  // Second, not last: it is the one link here about what is being asked
  // for this month, and it removes itself when the last date under it has
  // gone. `title` is the section's own, so the tooltip is not the label.
  ...(promoted
    ? [{ href: `/${promoted.slug}/`, label: promoted.label, title: promoted.title, icon: "star" }]
    : []),
  { href: "/news/", label: "News", title: "New York news, with a source on every entry", icon: "news" },
  { href: "/events/", label: "Events", title: "The New York events calendar", icon: "events" },
  { href: "/about/", label: "About", title: "Who publishes this and how every date is checked", icon: "id" },
];

---
<BaseLayout
  {...site}
  prose={prose}
  title={title}
  description={description}
  nav={nav}
  jsonLd={jsonLd}
  image={image}
  noindex={noindex}
>
  <slot />
</BaseLayout>
